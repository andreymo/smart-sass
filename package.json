{
  "name": "smart-sass",
  "version": "1.0.0",
  "description": "Sass compilation optimizer, decides which files need to be compiled",
  "main": "./lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "sass",
    "precompiler"
  ],
  "author": "Andrey Moiseev",
  "license": "MIT",
  "dependencies": {
    "bluebird": "^3.4.1",
    "fs-extra": "^0.30.0",
    "glob": "^7.0.5"
  }
}
